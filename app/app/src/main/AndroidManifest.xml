<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bsi.fbd.minisiga">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.adm.ProfessorRegisterAdm"></activity>
        <activity android:name=".gui.adm.AlunoRegisterAdm" />
        <activity android:name=".gui.adm.BlocoRegisterAdm" />
        <activity
            android:name=".gui.adm.Main_Adm_Activity"
            android:label="@string/title_activity_main_adm_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".gui.adm.RegisterActivity" />
        <activity android:name=".gui.main.Network" />
        <activity android:name=".gui.main.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>